#pragma once
/**
*	@desc:					Process data received from server (if data have both opcode and dataFile)
*	@param	recvData	    Data splitted by function bytestream
*/
void processData(RecvData &recvData) {
	switch (recvData.opcode)
	{
		default:
			break;
		}
}